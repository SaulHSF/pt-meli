# ===========================================
# Variables para despliegue con Docker
# ===========================================
# Copiar a .env y ajustar según el entorno:
# cp .env.example .env
#
# Si los puertos 3000/3001 están ocupados, usar otros (ej: 3010/3011)

# ---- API ----
# Origen(es) permitidos por CORS (separar por coma si hay varios)
# Debe ser la URL desde la que se sirve el frontend
FRONTEND_URL=http://localhost:3010

# Puerto donde se expone el API en el host
API_PORT=3011

# ---- Frontend ----
# URL pública del API (la que usará el navegador para fetch)
# Debe coincidir con el puerto/dominio donde se expone el API
VITE_API_URL=http://localhost:3011

# Puerto donde se expone el frontend en el host
FRONTEND_PORT=3010
